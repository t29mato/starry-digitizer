// TODO: Defining styles of points and axis only to discontinue Pinia.
// Should be managed in an appropriate layer as variables (not constants), but I cannot dicide where it is right now.

export const STYLE = {
  pointSizePx: 10,
  tempPointSizePx: 8,
  axisSizePx: 20,
  pointOpacity: 0.7,
  tempPointOpacity: 0.4,
}
