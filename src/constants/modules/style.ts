// TODO: Defining styles of plots and axis only to discontinue Pinia.
// Should be managed in an appropriate layer as variables (not constants), but I cannot dicide where it is right now.

export const STYLE = {
  plotSizePx: 10,
  tempPlotSizePx: 8,
  axisSizePx: 20,
  axisOpacity: 0.5,
  plotOpacity: 0.7,
  tempPlotOpacity: 0.4,
}
